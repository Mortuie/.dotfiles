alias clean="docker system prune"
alias dc="docker-compose"
alias dcb="docker-compose build"
alias dcu="docker-compose up"
alias dk="docker kill"
alias dp="docker ps"
alias dei="docker exec -it"
alias dr="docker run"
alias dcl="docker container list"

# kubernetes aliases
alias k="kubectl"


# git aliases
alias savecred="git config credential.helper store && echo \"Should store now\""
alias gituser="git config --global user.email \"jimmybob-leon-leon@hotmail.co.uk\" && git config --global user.name \"mortuie\""

alias ggs="git status"

# vscode aliases
alias code="code-insiders"
